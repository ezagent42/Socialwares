{
  "name": "socialwares",
  "private": true,
  "workspaces": ["app"],
  "scripts": {
    "dev:app": "pnpm --filter app run dev",
    "build:app": "pnpm --filter app run build"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
