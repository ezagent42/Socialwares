# Conversation test cases for the default role.
# Each case: what user says → what agent should do.
# Used by: run_auto.py
#
# Add more files per role: reviewer.yaml, admin.yaml, etc.

role: default
cases:
  - input: "check health"
    expected_skill: check_health
    description: "Agent should use check_health skill"

  # Add more as your app grows:
  # - input: "create a task: fix login bug"
  #   expected_skill: create_task
  #   expected_contains: "task"
  #   description: "Agent creates task via skill"
  #
  # - input: "list tasks"
  #   expected_skill: list_tasks
  #   description: "Agent lists tasks via skill"
